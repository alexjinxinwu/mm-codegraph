## 1. 外壳布局

- [x] 1.1 搭建全局布局:顶部搜索栏、主画布区域、状态区三个持久区域
- [x] 1.2 画布区域暴露「接收种子节点」的接口(本切片只接收并占位展示,不渲染图)
- [x] 1.3 布局在窄屏(移动端)下可用,搜索栏不挤压画布区域

## 2. schema 选择

- [x] 2.1 提供 schema 选择器,取值来自允许列表
- [x] 2.2 schema 作为后续 /resolve 调用的参数;未选 schema 时禁用搜索提交

## 3. 搜索控件

- [x] 3.1 kind 选择器,选项镜像 ENTRY_RESOLVERS 已注册入口(commandId / flowId)
- [x] 3.2 value 输入框
- [x] 3.3 提交时校验 kind 与 value 非空,否则不发请求并就地提示
- [x] 3.4 以 schema + kind + value 调用 /resolve

## 4. 三态渲染

- [x] 4.1 found → 取 roots[0] 作为种子交给画布区域
- [x] 4.2 multiple → 渲染候选选择器,逐个展示 title / subtitle
- [x] 4.3 候选被选中 → 该候选作为种子交给画布区域
- [x] 4.4 notFound → 渲染空状态提示,不作为错误处理

## 5. 加载与错误态

- [x] 5.1 请求进行中渲染加载态,期间禁止重复提交
- [x] 5.2 网络 / 服务端错误渲染错误态,与 notFound 的空状态区分开
- [x] 5.3 错误态提供重试入口

## 6. 测试

- [x] 6.1 found 单命中 → 根节点作为种子出现在画布区域
- [x] 6.2 multiple → 候选选择器列出全部候选,各含 title / subtitle
- [x] 6.3 从候选中选定一项 → 该项作为种子交给画布
- [x] 6.4 notFound → 显示空状态,无错误样式
- [x] 6.5 服务端错误 → 显示错误态与重试,且不与 notFound 混淆
- [x] 6.6 未选 schema / value 为空 → 提交被拦截,不发请求
- [x] 6.7 加载中重复点击提交 → 仅触发一次请求
- [x] 6.8 窄屏布局 → 三区域均可访问,无横向溢出
